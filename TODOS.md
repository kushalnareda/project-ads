# TODOS — @project-ads MVP

Generated by /plan-eng-review on 2026-07-03

## P0 — Gate (do before writing code)

- [x] Gate Zero: verify `spinnerVerbs` rewrite takes effect mid-session — FAILED (Claude Code caches settings.json at startup). Spinner injection works at session-open only, not per-prompt. StatusLine is primary real-time surface.

## P1 — Must ship (T1–T7)

- [ ] **T1** Server: `/v1/publisher/register` endpoint — `POST { email } → { publisher_token: uuid }`, idempotent via `publishers/{sha256(email)}.json` in R2
- [ ] **T2** Server: add `publisher_token` + `tool: "claude-code"` to impression R2 objects; surface-dependent `credits_delta` (0.001 spinner / 0.0002 statusLine)
- [ ] **T3** Server: add vitest + test `/v1/publisher/register` with mocked S3Client
- [ ] **T4** Hook: write `~/.project-ads/hook.js` — UserPromptSubmit hook (publisher_token, spinner cache, last-active.json, settings.json rewrite, migrate old hook entry)
- [ ] **T5** StatusLine: update `~/.claude/hooks/project-ads-statusline.mjs` — separate cache file (`current-ad-statusline.json`), 5-min idle gate via `last-active.json`
- [ ] **T6** Setup CLI: build `packages/setup/` — `npx @project-ads/setup` registers publisher, writes token, wires hooks, removes old `project-ads-hook.js` entry
- [ ] **T7** CI: add `.github/workflows/setup-publish.yml` for `@project-ads/setup` npm publish
- [ ] Deploy T1+T2 to Fly.io **before** publishing T6 to npm (server must have publisher_token before publishers register)

## P2 — Ship before first paid advertiser

- [ ] Admin form (password-protected static HTML) to update ad config and view impression count per publisher — required before recruiting second advertiser
- [ ] R2 write failure alerting: log error server-side but always return 200 to SDK
- [ ] Server-side content validation for `ad_text` and `url` before serving to terminal
- [ ] Document that 5s rate limit is per-process (CI parallel agents each get own window)
- [ ] Fix `display.ts` OSC 8 bug: `colorterm === 'truecolor'` checked twice, `'24bit'` branch unreachable — silent fallback to plain text for some terminals

## Post-MVP

- [ ] `wait_duration_ms` via Stop hook: link impression UUID across UserPromptSubmit + Stop events, compute generation duration, enables per-duration pricing for advertisers
- [ ] Server-side rate limit keyed on hostname + user hash (covers multi-process CI scenarios)
- [ ] Cross-machine wallet sync (requires server-side ledger — depends on Stripe Connect)
- [ ] Publisher token rotation / security hardening
- [ ] Context-aware targeting — SDK v2: embedder passes `tags: ['postgres', 'python']`
- [ ] VS Code / Codex hook support (different hook system)
- [ ] Real-time bidding engine
- [ ] Advertiser self-serve portal
