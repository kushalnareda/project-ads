# TODOS — @muscat/agent-ads MVP

Generated by /plan-eng-review on 2026-06-29

## P1 — Must ship

- [ ] Atomic wallet writes: write to `~/.muscat-ads/wallet.tmp`, fsync, rename to `wallet.json`
- [ ] R2 write failure alerting: log error server-side but always return 200 to SDK
- [ ] Server-side content validation for `ad_text` and `url` before serving to terminal
- [ ] Create `muscat` npm org at npmjs.com before first publish

## P2 — Ship before first paid advertiser

- [ ] Add `tool` (e.g. `"claude-code"`) and `wait_duration_ms` to impression payload — makes inventory legible to advertisers
- [ ] Admin form (password-protected static HTML) to update ad config and view impression count — required before recruiting second advertiser
- [ ] Document that 5s rate limit is per-process (CI parallel agents each get own window)

## Post-MVP

- [ ] Server-side rate limit keyed on hostname + user hash (covers multi-process CI scenarios)
- [ ] Cross-machine wallet sync (requires developer identity/account system)
- [ ] Context-aware targeting — SDK v2: embedder passes `tags: ['postgres', 'python']`
- [ ] Credit exchange — map `credits_delta` to real AI usage credits
- [ ] Real-time bidding engine
