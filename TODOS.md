# TODOS — @project-ads MVP

Generated by /plan-eng-review on 2026-07-03

## P0 — Gate (do before writing code)

- [x] Gate Zero: verify `spinnerVerbs` rewrite takes effect mid-session — FAILED (Claude Code caches settings.json at startup). Spinner injection works at session-open only, not per-prompt. StatusLine is primary real-time surface.

## P1 — Must ship (T1–T7)

- [x] **T1** Server: `/v1/publisher/register` endpoint — `POST { email } → { publisher_token: uuid }`, idempotent via `publishers/{sha256(email)}.json` in R2
- [x] **T2** Server: add `publisher_token` + `tool: "claude-code"` to impression R2 objects; surface-dependent `credits_delta` (0.001 spinner / 0.0002 statusLine)
- [x] **T3** Server: add vitest + test `/v1/publisher/register` with mocked S3Client
- [x] **T4** Hook: write `~/.project-ads/hook.js` — UserPromptSubmit hook (publisher_token, spinner cache, last-active.json, settings.json rewrite, migrate old hook entry)
- [x] **T5** StatusLine: update `~/.claude/hooks/project-ads-statusline.mjs` — separate cache file (`current-ad-statusline.json`), 5-min idle gate via `last-active.json`
- [x] **T6** Setup CLI: build `packages/setup/` — `npx @project-ads/setup` registers publisher, writes token, wires hooks, removes old `project-ads-hook.js` entry
- [x] **T7** CI: add `.github/workflows/setup-publish.yml` for `@project-ads/setup` npm publish
- [ ] Deploy T1+T2 to Fly.io **before** publishing T6 to npm (server must have publisher_token before publishers register)

## P2 — Ship before first paid advertiser

- [x] Admin form (password-protected static HTML) to update ad config and view impression count per publisher — `/admin` console (2026-07-07)
- [x] R2 write failure alerting: structured JSON-lines logs (`r2.*_failed` events) — grep/drain `fly logs` on `"level":"error"`; SDK still gets 200
- [x] Server-side content validation for `ad_text` and `url` before serving to terminal (ANSI/control chars stripped at write + serve, 160-char cap, http(s)-only URLs)
- [x] Document that 5s rate limit is per-process (CI parallel agents each get own window) — see README; server also enforces 60/min per publisher token
- [x] Fix `display.ts` OSC 8 bug: `colorterm === 'truecolor'` checked twice, `'24bit'` branch unreachable — silent fallback to plain text for some terminals

## Hardening shipped 2026-07-07 (feat/payout-engine)

- [x] Per-key serialization of ledger credit + campaign spend R2 read-modify-write (no lost credits under concurrency; single-instance deploy assumption)
- [x] Publisher token / campaign id UUID validation (R2 key-namespace traversal closed)
- [x] Server-side rate limits: 60/min per token `/v1/impression`, 10/hr per IP `/v1/publisher/register`
- [x] Timing-safe admin token comparison
- [x] Payout idempotency by external payment `reference`
- [x] Structured JSON logging + 5xx middleware

## Post-MVP

- [ ] `wait_duration_ms` via Stop hook: link impression UUID across UserPromptSubmit + Stop events, compute generation duration, enables per-duration pricing for advertisers
- [ ] Server-side rate limit keyed on hostname + user hash (covers multi-process CI scenarios)
- [ ] Cross-machine wallet sync (requires server-side ledger — depends on Stripe Connect)
- [ ] Publisher token rotation / security hardening
- [ ] Context-aware targeting — SDK v2: embedder passes `tags: ['postgres', 'python']`
- [ ] VS Code / Codex hook support (different hook system)
- [ ] Real-time bidding engine
- [ ] Advertiser self-serve portal
